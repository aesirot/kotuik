<hibernate-configuration>
    <session-factory>
        <!-- JDBC Database connection settings -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://10.128.0.19:5432/postgres</property>
        <property name="connection.username">postgres</property>
        <property name="connection.password">krmdrnkpswPG</property>
        <!-- JDBC connection pool settings ... using built-in test pool -->
        <property name="connection.pool_size">10</property>
        <!-- Select our SQL dialect -->
        <property name="dialect">org.hibernate.dialect.PostgreSQL95Dialect</property>
        <!-- Echo the SQL to stdout -->
        <property name="show_sql">false</property>
        <!-- Set the current session context -->
        <property name="current_session_context_class">thread</property>
        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>
        <!-- dbcp connection pool configuration -->
        <property name="hibernate.dbcp.initialSize">1</property>
        <property name="hibernate.dbcp.maxTotal">10</property>
        <property name="hibernate.dbcp.maxIdle">3</property>
        <property name="hibernate.dbcp.minIdle">0</property>
        <property name="hibernate.dbcp.maxWaitMillis">-1</property>

        <mapping class="model.Trade" />
        <mapping class="model.Bond" />
        <mapping class="model.LegalEntity" />
        <mapping class="model.BidAskLog" />
        <mapping class="model.RobotState" />
        <mapping class="model.integration.MoexTrade" />
    </session-factory>
</hibernate-configuration>